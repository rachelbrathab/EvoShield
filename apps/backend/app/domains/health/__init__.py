"""Health domain — liveness and dependency probes.

The reference implementation of the domain pattern: business logic in
`service.py`, API contract in `schemas.py`. The HTTP router in
`app/api/routers/health.py` adapts this domain to REST.
"""
