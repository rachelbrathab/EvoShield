{
  "name": "evoshield",
  "version": "0.1.0",
  "private": true,
  "description": "Predictive Software Supply Chain Risk Assessment Using Temporal Repository Intelligence",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "dev": "bash scripts/dev.sh",
    "dev:frontend": "npm --prefix apps/frontend run dev",
    "dev:backend": "bash -c 'cd apps/backend && uv run uvicorn app.main:app --reload --port 8000'",
    "lint": "npm --prefix apps/frontend run lint",
    "typecheck": "npm --prefix apps/frontend run typecheck",
    "build:frontend": "npm --prefix apps/frontend run build",
    "test:backend": "bash -c 'cd apps/backend && uv run pytest'",
    "lint:backend": "bash -c 'cd apps/backend && uv run ruff check .'",
    "format:backend": "bash -c 'cd apps/backend && uv run ruff format .'"
  }
}
